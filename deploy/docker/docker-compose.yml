name: virki-control-plane

services:
  control-plane-backend:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile.backend
    container_name: virki-control-plane-backend
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - GCP_PROJECT=${GCP_PROJECT:-strong-zephyr-505611-k4}
      - GCP_ZONE=${GCP_ZONE:-europe-west3-a}
      - GCP_REGION=${GCP_REGION:-europe-west3}
      - GITHUB_TOKEN=${GITHUB_TOKEN:-}
      - PATH=/opt/google-cloud-sdk/bin:/usr/local/bin:/usr/bin:/bin
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - control-plane-data:/app/data
      - /tmp/virki_instances:/tmp/virki_instances
      - /home/peter/.config/gcloud:/root/.config/gcloud
      - /home/peter/.local/share/google-cloud-sdk:/opt/google-cloud-sdk:ro
    networks:
      - virki-net

  control-plane-web:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile.web
    container_name: virki-control-plane-web
    restart: unless-stopped
    ports:
      - "8280:8280"
    environment:
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-http://localhost:8080}
    depends_on:
      - control-plane-backend
    networks:
      - virki-net

volumes:
  control-plane-data:

networks:
  virki-net:
    driver: bridge
